// ---- GENERATED BY QEDGEN — DO NOT EDIT ---- spec-hash:64f57647298569b4
use anchor_lang::prelude::*;

#[repr(C)]
#[derive(AnchorSerialize, AnchorDeserialize, InitSpace, Clone, Copy, Debug, PartialEq)]
pub struct State {
    pub policy_status: u8,
    pub emergency_pause: u8,
    pub max_amount_per_period: u64,
    pub max_chunk_amount: u64,
    pub period_length_seconds: u64,
    pub current_period_start: u64,
    pub current_period_total: u64,
    pub pulled_amount: u64,
    pub payment_amount: u64,
    pub gateway_fee_bps: u16,
    pub protocol_share_bps: u16,
    pub scheduler_share_bps: u16,
    pub referral_allocation_bps: u16,
    pub is_referral_enabled: u8,
    pub is_net_mode: u8,
    pub total_fee: u64,
    pub protocol_cut: u64,
    pub scheduler_cut: u64,
    pub referral_pool: u64,
    pub gateway_residual: u64,
    pub recipient_amount: u64,
    pub total_from_user: u64,
    pub release_due_date: u8,
    pub release_requires_gateway: u8,
    pub release_requires_owner: u8,
    pub release_requires_recipient: u8,
    pub caller_is_gateway: u8,
    pub caller_is_owner: u8,
    pub caller_is_recipient: u8,
}

#[account]
pub struct TributaryAccount {
    pub policy_status: u8,
    pub emergency_pause: u8,
    pub max_amount_per_period: u64,
    pub max_chunk_amount: u64,
    pub period_length_seconds: u64,
    pub current_period_start: u64,
    pub current_period_total: u64,
    pub pulled_amount: u64,
    pub payment_amount: u64,
    pub gateway_fee_bps: u16,
    pub protocol_share_bps: u16,
    pub scheduler_share_bps: u16,
    pub referral_allocation_bps: u16,
    pub is_referral_enabled: u8,
    pub is_net_mode: u8,
    pub total_fee: u64,
    pub protocol_cut: u64,
    pub scheduler_cut: u64,
    pub referral_pool: u64,
    pub gateway_residual: u64,
    pub recipient_amount: u64,
    pub total_from_user: u64,
    pub release_due_date: u8,
    pub release_requires_gateway: u8,
    pub release_requires_owner: u8,
    pub release_requires_recipient: u8,
    pub caller_is_gateway: u8,
    pub caller_is_owner: u8,
    pub caller_is_recipient: u8,
}
// ---- END GENERATED ----

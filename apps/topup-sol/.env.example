# Solana RPC endpoint. Defaults to a local Surfpool instance.
# Start Surfpool with:
#   surfpool start --legacy-anchor-compatibility --no-tui
VITE_SOLANA_API=http://localhost:8000

# Optional devnet override for the cluster switcher.
VITE_SOLANA_API_DEVNET=https://api.devnet.solana.com
